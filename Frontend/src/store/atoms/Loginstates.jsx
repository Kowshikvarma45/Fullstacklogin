import {atom} from "recoil"

export const LoginButtonAtom = atom({
    key:"LoginButtonAtom",
    default:"Show Password"
})

export const LogindivAtom = atom({
    key:"LogindivAtom",
    default:""
})